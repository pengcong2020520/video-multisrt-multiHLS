# Spec · video-multisrt-multi-hls

> ★★★ 技术规格 + 开发规范。阶段② 定稿后，**阶段③ 开发期间不可改动**。
> 如确需变更：停止开发 → 回阶段② 修订 Spec → 版本号 +1 → 再继续。

**版本**：v1.0　**定稿日期**：2026-06-30　**状态**：DRAFT / FROZEN

---

## 1. 系统架构
- 架构风格（单体/分层/微服务/前后端分离…）：
- 架构图（文字或 mermaid）：
- 技术栈：语言 / 框架 / 运行时 / 数据库 / 部署

## 2. 模块定义（开发分工的契约）
> 每个模块是一个 Agent 的工作单元。模块间**只能**通过这里定义的接口交互。

### 模块 M1 · <名称>
- 职责边界：
- 目录：`src/modules/<m1>/`
- 对外接口：见 §4
- 依赖：
- 验收标准：

（按需复制更多模块）

## 3. 数据库设计
- 存储选型：
- 表/集合 schema：
  ```
  table xxx { id, ..., created_at }
  ```
- 索引 / 约束 / 迁移策略：

## 4. 接口契约（API）
> 模块间 & 前后端的唯一交互口径。字段、类型、错误码必须明确。

| 接口 | 方法/路径 | 入参 | 出参 | 错误码 | 提供方模块 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

## 5. 开发规范（所有 Agent 必须遵守）
- **代码风格**：命名 / 格式化工具 / lint 规则
- **目录约定**：模块内部结构、文件命名
- **错误处理**：统一错误模型、日志规范
- **测试要求**：每模块单测覆盖、集成测试入口
- **提交规范**：commit message 格式、分支策略、PR 要求
- **依赖管理**：新增依赖的审批口径
- **禁止事项**：不得跨模块直接改他人代码；不得绕过接口契约；不得修改本 Spec

## 6. 安全与配置
- 密钥/配置管理：
- 鉴权/权限模型：

## 7. 非功能约束
- 性能目标 / 并发 / 资源上限：

## 8. 变更记录
| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-06-30 | 初版定稿 |
